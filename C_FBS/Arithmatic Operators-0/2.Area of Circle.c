#include<stdio.h>

void main()
{
	//area of circle = pi*r*r
	float pi=3.14;
	int radius;
	float Area;
	radius=5;
	Area=pi*radius*radius;
	printf("Area of circle having radius %d is %.2f",radius,Area);
	
	
}
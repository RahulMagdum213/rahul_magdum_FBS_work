#include<stdio.h>
 void main()
 {
 	// area of triangle is 1/2 * base * height
 	
 	int b,h,a;
 	b=3;
 	h=6;
 	a=0.5*b*h;
 	printf("Area of trianglr having base = %d and height =%d is %d",b,h,a);
 	//taked 0.5 cause as in c if i take 1/2 its doint first int/int = int answer which is 1/2=0 so always take 0.5
 	
 }